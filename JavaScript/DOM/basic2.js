let a= document.querySelector("#k1");
console.dir(a);
// a.style.backgroundColor="blue";

let b= document.querySelector("p");
// a.append(b);     end (inside)
// a.prepend(b);    start (inside)
// a.before(b);     before (outside)
// a.after(b);      after  (outside)

// b.remove();
